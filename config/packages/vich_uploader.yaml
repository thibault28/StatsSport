vich_uploader:
  db_driver: orm

  mappings:
    examples:
      uri_prefix: /uploads/examples
      upload_destination: "%kernel.project_dir%/public/uploads/examples"
      namer: vich_uploader.namer_uniqid
    machines:
      uri_prefix: /uploads/machines
      upload_destination: "%kernel.project_dir%/public/uploads/machines"
      namer: vich_uploader.namer_uniqid
